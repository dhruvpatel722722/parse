version = "1.0"

[metadata]
name = "stream-reconstruct"
description = "Fix interacting bugs in a WAL-based log compaction pipeline"
category = "debugging"
tags = ["distributed-systems", "wal", "compaction", "recovery", "python"]
author_name = "dhruvpatel"
difficulty = "hard"
expert_time_estimate_min = 60
junior_time_estimate_min = 180

[environment]
allow_internet = true
timeout = 1800

[agent]
instruction_path = "instruction.md"

[verifier]
test_command = "bash /app/tests/test.sh"
