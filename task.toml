[task]
id = "broken-package-resolver"
name = "Broken Package Resolver"
description = "Reconstruct the correct package installation order from a corrupted package manager database with version-conditional dependencies, mutual exclusion constraints, virtual provides, and epoch-based versioning."
difficulty = "hard"

[environment]
allow_internet = true
timeout = 1800

[solution]
script = "solution/solve.sh"
