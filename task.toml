version = "1.0"

[metadata]
name = "stream-reconstruct"
description = "Recover records from a binary stream scrambled by block-level byte shuffle and repeating XOR mask"
category = "reverse_engineering"
tags = ["cryptanalysis", "binary", "permutation", "signal-processing"]
author_name = "dhruvpatel"
difficulty = "hard"
expert_time_estimate_min = 45
junior_time_estimate_min = 120

[environment]
allow_internet = true
timeout = 1800

[agent]
instruction_path = "instruction.md"

[verifier]
test_command = "bash /app/tests/test.sh"
